# SQL — Financial datasets and queries

🚧 **Coming soon** — Currently working through DataCamp's SQL Fundamentals track.

## Planned content

- SQLite database with fictional portfolio data: `funds`, `holdings`, `prices`, `trades`, `benchmarks`
- Query exercises by topic:
  - `SELECT` + `WHERE` + `GROUP BY` + aggregations
  - Joins (inner, left, anti)
  - Subqueries and CTEs
  - Window functions (`ROW_NUMBER`, `LAG`, `LEAD`, `SUM OVER`)
  - Date functions and time-series queries
- Interview-style exercises typical of buy-side technical screenings

Updates expected in the coming weeks.
